<img src="../public/assets/img/burger.png" alt="Burger image">


<h1>Eat-Da-Burger</h1>


<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers are Change me!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger/burger-devoured devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>

<div class="display">
  <form class="create-form">

  <div class="form-group">
    <textarea id="quo" for="ba" name="quote" rows="8" cols="40"></textarea>
  </div>
  <button type="submit">Add Burger</button>
  <p>Enter a burger you want to Eat. Then click Add Burger Button</p>
</form>
</div>